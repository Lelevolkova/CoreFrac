# Core runtime for the standalone scripts (distillation, inference, eval, zero-shot).
numpy>=1.24
pillow>=9.5

# UNet distillation / inference (distill_unet.py, infer_unet.py)
torch>=2.0
torchvision>=0.15

# SAM 3 (Sam3Model / Sam3Processor) and the zero-shot open-vocabulary baselines
# (CLIPSeg, OWLv2, Florence-2) all come from HuggingFace transformers.
transformers>=5.0.0

# Matched-cap rank-8 LoRA baseline (code/train_sam3_lora.py).
peft>=0.11
accelerate>=0.30
pyyaml>=6.0
scipy>=1.10
# Optional: FlashAttention 2, used when model.use_flash_attention is true.
# flash-attn

# cluster_bootstrap.py needs only numpy and the standard library.

# NOTE: the prompt-evolution loop (scripts/run_evolution.sh, problems/.../test.py)
# additionally requires the GigaEvo evolutionary framework, a SAM3 checkpoint,
# an OpenAI-compatible LLM inference endpoint, and Redis. See README.md.
